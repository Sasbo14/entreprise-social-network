# 🌐 Réseau Social d'Entreprise

Monorepo contenant :

- `backend/` : API Java avec Spring Boot
- `frontend/` : SPA React avec Vite

## 🚀 Démarrer

### Backend

```bash
cd backend
./mvnw spring-boot:run
